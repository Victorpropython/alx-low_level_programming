#include "main.h"
/**
 * is_palindrome - To print an empty string
 * @s: char variable
 * Return: 1 if palindrome or 0 of n9t
 */
int is_palindrome(char *s)
{
	char c;

	if (*s != '\0')
	{
		return (0);
	}
	return (1);
}

