#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>

int main(int __attribute__((__unused__)) argc, char *argv[]);
int _putchar(char c);
int main(int argc, char *argv[]);

#endif
