#include "main.h"
/**
 *_abs - To get the absolute  of a number
 *@n: checker for int
 *@-n: value for negative number
 * Return: Always 0
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
		return (-n);
}


