#include "main.h"
/**
 * *_strcpy - To search for a string for any of the set byte of character
 * @src: initail segement
 * @dest: to accept the byte
 * Return: Always success
 */
char *_strcpy(char *dest, char *src)
{
	return (_strcpy(dest, src));
}
